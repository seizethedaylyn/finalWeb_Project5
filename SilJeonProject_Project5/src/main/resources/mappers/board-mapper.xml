<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Board">
    <insert id = "insertBoard">
        insert into BOARD (name, birth, team, snsId, awards, position, song, height, weight)
<!--        name varchar(20)-->
<!--        birthday date-->
<!--        team varchar(30)-->
<!--        snsId varchar(50)-->
<!--        awards varchar(1000)-->
<!--        position varchar(30)-->
<!--        song varchar(50)-->
<!--        height varchar(20)-->
<!--        weight varchar(20)-->
    </insert>
    <select id="getBoardList" resultType="board">
        select * from BOARD2 where id = #{seq}
    </select>

</mapper>